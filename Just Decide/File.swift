//
//  File.swift
//  Just Decide
//
//  Created by James Jones on 25/07/2020.
//  Copyright © 2020 James Jones. All rights reserved.
//

import Foundation
/*  func createSlices() -> [CarnivalWheel] {
      var newSlices: [CarnivalWheel] = []
      
      let slice1 = CarnivalWheel(title: "Pizza")
      let slice2 = CarnivalWheel(title: "Chinese")
      let slice3 = CarnivalWheel(title: "Indian")
      let slice4 = CarnivalWheel(title: "KFC")
      let slice5 = CarnivalWheel(title: "Mcdonalds")
      let slice6 = CarnivalWheel(title: "Steak")
      
      
      newSlices.append(slice1)
      newSlices.append(slice2)
      newSlices.append(slice3)
      newSlices.append(slice4)
      newSlices.append(slice5)
      newSlices.append(slice6)
      
      PersistenceManager.updateWith(slice: slice1, actionType: .add) { [weak self] error in
          guard let self = self else {return}
          guard let error = error else {
         print("Successs")
              return
          }
          print("Something went wrong")
      }
      
      return newSlices
  } */
